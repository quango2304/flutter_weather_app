name: weather_app
description: Flutter weather project

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_dotenv: ^5.1.0
  bloc: ^8.1.2
  flutter_bloc: ^8.1.3
  equatable:
  stream_transform:
  http:
  injectable:
  get_it:
  lottie:
  intl:
  meta:
  google_fonts:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner:
  injectable_generator:
  bloc_test:
  mocktail:
flutter:
  uses-material-design: true
  assets:
    - env/.env
    - assets/lotties/